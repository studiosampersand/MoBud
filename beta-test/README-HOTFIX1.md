# MoBud v0.000.008 — Hotfix 1

Beta-only replacement package for `/beta-test/`.

Fixes:
- Full-interface light mode is applied immediately and persisted.
- Theme is applied before CSS/JavaScript rendering to prevent stale dark rendering.
- Cache-busted assets and a new service-worker cache.
- Mobile Garage bottom-navigation and horizontal-overflow fix.
- Responsive FAQ items on narrow screens.
- Existing automatic Drive sync, backup actions, last-sync display and multi-device warning remain included.

No files outside `/beta-test/` are required for this hotfix.
